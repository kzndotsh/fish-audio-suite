# `packages/kit/tests`

> Inherits [`../AGENTS.md`](../AGENTS.md)

`uv run pytest packages/kit`. `--import-mode=importlib` is set at the workspace root.

Cue aliases, junk, ASR drop, and `next_tts_cut` live in [`test_kit.py`](test_kit.py).
