# `nix/`

> Inherits root [AGENTS.md](../AGENTS.md)

| File | Owns |
| --- | --- |
| [`module.nix`](module.nix) | `nixosModules.default` — OCI `fish-audio-suite-proxy`, `127.0.0.1:8849:8849`, `autoStart = false` |

Flake outputs and PortAudio wrap live in [`../flake.nix`](../flake.nix), not here.
